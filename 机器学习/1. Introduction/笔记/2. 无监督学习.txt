无监督学习

无监督学习使我们能够处理问题，很少或不知道我们的结果应该是什么样子。 我们可以从数据中导出结构，其中我们不一定知道变量的影响。

我们可以通过基于数据中的变量之间的关系对数据进行聚类来导出此结构。

对于无监督学习，没有基于预测结果的反馈。

例：

聚类：采集100万个不同基因的集合，并找到一种方法来自动将这些基因分组到不同变量（如寿命，位置，角色等）中相似或相关的组中。

非聚类：“鸡尾酒会算法”，允许你在混乱的环境中找到结构。 （即，在鸡尾酒会上从声音网中识别单个声音和音乐）。